<h1><%=@post.user_id %></h1>
<p><%= @post.content %></p>
<p><%= @post.is_edited %></p>
<%= link_to "Destroy", post_path(@post), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %>
<%= link_to "BACK", g_user_path%>