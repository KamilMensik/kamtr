<% @users.each do |i| %>
  <%= i.username %><br/>
  <%= i.email %><br/>
  <%= link_to "Delete this account!",user_path(i), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %><br/>
<% end %>